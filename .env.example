# Base URL of the API, including /api
VITE_API_URL=http://localhost:5050/api

# Show the Student / Industry / Academician / Institution demo switcher in the top bar (set false to hide)
VITE_DEMO_SWITCH=true
